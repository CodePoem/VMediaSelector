<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vdreamers.vmediaselector.ui" >

    <application>
        <provider
            android:name="com.vdreamers.vmediaselector.ui.MediaSelectorFileProvider"
            android:authorities="${applicationId}.vmediaselector.file.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/v_selector_ui_default_file_provider"/>

        </provider>
    </application>

</manifest>
